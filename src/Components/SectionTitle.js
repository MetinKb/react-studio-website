function SectionTitle({ title, content }) {
    return (
        <div className="section-title-container">
            <h2>{title}</h2>
            <p>{content}</p>
        </div>
    )
}

export default SectionTitle